import 'package:flutter/material.dart';
import 'package:tim_tictactoe/widgets/game_ui.dart';

class ScoreBoard extends StatelessWidget {
  const ScoreBoard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const GameUi();
  }
}
